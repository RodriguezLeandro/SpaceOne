using UnityEngine;

public class EnemySpaceshipLifeScript : MonoBehaviour
{

    public float lifeCapacity = 3f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
